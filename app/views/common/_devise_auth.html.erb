<ul class="nav navbar-nav pull-right">
  <li>
    <%= link_to('Log in', new_user_session_path) if !user_signed_in? %>
  </li>
  <li>
    <%= link_to('Register', new_user_registration_path) if !user_signed_in? %>
  </li>
  <li>
    <%= link_to current_user.full_name, edit_user_registration_path if user_signed_in? %>
  </li>
  <li>
    <%= link_to('Log out', destroy_user_session_path, :method => :delete) if user_signed_in? %>
  </li>
</ul>