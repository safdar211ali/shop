I"E(function() {
  var ready;

  ready = function() {
    if ($("#form-product").length > 0) {
      $(".submit-btn").click(function(event) {
        return event.preventDefault();
      });
      return $("#form-product").submit();
    }
  };

  $(document).ready(ready);

  $(document).on("page:load", ready);

}).call(this);
:ET